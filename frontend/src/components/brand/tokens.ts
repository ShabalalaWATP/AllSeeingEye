/** The page ground colour, used as the Evil Eye background so the mark blends in. */
export const BRAND_GROUND = '#07070b';

/** Accessible name shared by every instance of the mark. */
export const BRAND_NAME = 'The All Seeing Eye';
