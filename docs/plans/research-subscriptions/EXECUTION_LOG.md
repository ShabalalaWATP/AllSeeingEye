# Research and Subscriptions execution log

This log tracks implementation against the [approved plan](../../RESEARCH_SUBSCRIPTIONS_IMPLEMENTATION_PLAN.md). Only completed work with evidence may be marked Complete in the plan. Results here are local development checks, not live-provider or human-quality acceptance.

## 14 September 2026: B01 baseline opened

- Branch `main`, baseline HEAD `fb2a591`. At the start of implementation, `git status --short` showed only the previously authored planning/audit documentation as changed or untracked. No application source changes were present in that initial status.
- The applicable global AGENTS.md was supplied in the user message. No repository or nested `AGENTS.md` files were found by `rg --files -g AGENTS.md`.
- `uv run alembic heads` returned `0034 (head)` without a database migration. A future schema task must recheck the head before naming a new revision.
- In a process-local test shell, `ASE_DATABASE_URL` and `ASE_TEST_DATABASE_URL` were forced to disposable in-memory SQLite and PostgreSQL test URL hooks cleared. `uv run pytest tests/test_research_collection.py tests/test_research_depth.py tests/test_research_replanning.py --no-cov -q` passed: 55 tests. Coverage was not measured.
- `uv run python -m evaluations validate` passed eight existing synthetic cases. Their reference rubrics are assistant-authored and not human reviewed. The two separate research replay cases and the proposed 60-case labelled set remain pending.
- The first sandboxed `uv` invocation could not read its user cache. A scoped escalation for the same safe test command succeeded. This is an execution environment issue, not a product test failure.
- Ownership for current work: `effective_query` agent owns C01 backend ranking; `schedule_outcome` owns C02 schedule backend/persistence/API; `historical_followup` owns C03 frontend handoff; root owns C03 backend parent-version/scope support, this log and integration. Workers were instructed not to revert one another's edits. C04 and later tasks have no editing owner yet.

Status: B01 complete. C01 and C02 are complete with the evidence below; C03/C04 and S01 are in progress. The rest of the approved plan remains pending.

## 14 September 2026: first correctness regressions

- C01: the effective-query worker reproduced the crowded Basic ranking defect with two failing regression cases. The admitted revised query, operator terms, variants and completed supplementary tasks now reach deterministic final selection and the checkpoint receipt. Offline focused and neighbouring suites passed 123 tests; Ruff, mypy and `git diff --check` passed. No live collection or model call was made.
- C02: the schedule worker made report/version outcome and coverage explicit; failed/review/partial versions no longer advance a successful baseline, and cosmetic edits preserve due slots. New focused regressions passed 9 tests; a broad compatibility suite passed 82 tests with one obsolete expectation, and its updated focused rerun passed 12 tests. Ruff and mypy passed. S01 will replace this direct-generation bridge with durable editions.
- C03 backend: direct and saved-map area follow-ups and recorded-time follow-ups now accept the exact scope and parent version. A selected older version remains the parent after a later version exists. Mismatched area, period or time basis is rejected before evidence reuse. These regression cases failed before the fix. Focused HTTP/unit tests passed 20 tests, a changed-polygon regression passed, then the broader follow-up/area/history suite passed 52 tests. Ruff check and format check passed for the edited backend files. C03 frontend and combined validation are still in progress.
- OpenAPI and generated frontend types were refreshed after the C02/C03 API contract changes. S01 may add one more coverage enum value, so regeneration is required again after its schema settles.
- S01 ownership: the schedule worker confirmed Alembic head `0034` and is implementing an additive ledger/migration against disposable test databases. No operator database migration has run.

Next integration action: finish C03/C04 frontend checks, verify exact-version snapshot behaviour and S01 migration, then start S02's report-job admission and freeze/restore seam. Human-labelled evaluation, current live sources/models, browser and export acceptance remain unverified.

## 14 September 2026: historical follow-up and durable edition integration

- C03/C04 frontend: selected report versions, historical dates and canonical area geometry now survive the follow-up handoff. Job and subscription summaries refresh at bounded visible-page intervals; form validation, delete confirmation and keyboard focus were repaired. The combined frontend run passed 97 tests across 21 files. Owned-file ESLint, Prettier, scoped TypeScript and `git diff --check` passed. Full application TypeScript was blocked by two errors in concurrently edited `cameraFrames.test.tsx`, outside these tasks. C03/C04 are complete against their local criteria; live/browser acceptance remains V03.
- S01: the additive `0035` edition ledger, frozen revisions and migration are implemented. The worker's focused suite passed 18 tests, with one disposable PostgreSQL migration fixture skipped. A populated old SQLite migration fixture passed. S01 stays in progress until PostgreSQL upgrade acceptance is evidenced.
- S02: report-job admission was split into preparation and a caller-owned transaction method, with no nested commit or rollback. Focused service tests passed 20 cases. Versioned subscription baseline snapshots, bounded seen fingerprints and reauthorisation before reuse passed a combined 47 snapshot/gate tests. Publication of report, job, edition and compatible coverage lineage now occurs within one final transaction. Out-of-order publication and gap tests passed 7 cases with neighbouring worker integration. The durable enqueuer and two-edition real-worker regression are still being integrated. No paid provider calls or live feeds were used in these tests.

Next integration action: finish S02 two-edition execution and capacity/authority regressions, then re-run static checks and the focused backend suites. The later S03–V04 packets remain pending.

## 2026-09-14: durable execution and shared-brief integration

- S02 now has one durable enqueue/worker/publication path. A two-edition test verifies separate jobs, frozen comparison context and saved reports; repeated ticks, rollback, capacity wait and revoked authority are covered by focused regressions. The former direct schedule producer was removed. S01's SQLite migration and populated-data checks pass; the optional disposable PostgreSQL migration fixture has not run.
- S03 worker retry uses the same edition/job/checkpoints, distinguishes known pre-dispatch transient failures from uncertain paid calls, closes an explicit pause attempt before a resumed claim, and stops at exhaustion or the 24-hour horizon. The 37-test retry/worker suite passed. UTC-month request/token budget enforcement is still being integrated.
- S04 has IANA local-time preview and due-slot/overlap/catch-up tests, including a populated SQLite migration. Its final schedule/budget seam is under test. Scheduled evidence intervals are anchored to the original due time; the synthetic two-edition fixture was corrected to place observations before that cutoff.
- S05's paginated edition-history API rechecks current session and object authority before release. The UI shows edition status, requested window, progress and report links. Control actions and a delivery outbox remain outstanding.
- S06's admission loop now starts independently of live feed polling; feed-disabled lifecycle tests passed. Administrator diagnostics and operating acceptance remain outstanding.
- R01 has a strict immutable Research Brief contract, insert-only revisions, scoped API, additive SQLite migration, exact one-off job admission, and frozen report-version provenance. All twelve authored requirement IDs and texts survive planning, checkpoint resume, synthesis, coverage and document export. Existing GeoJSON is canonicalised server-side, and report-origin provenance is distinct from inherited analytical follow-up. Unsupported choices fail admission rather than silently disappearing. R02's shared frontend editor is in progress; subscription admission and full maximal-brief parity remain outstanding.
- No-evidence research now produces a short needs-review abstention rather than a paused section error. The focused abstention tests and two-edition worker test passed. E00 capability inventory is in progress without live provider calls.

Next integration action: finish the S03 monthly budget seam and S04 schedule files, then connect exact Research Brief revisions to subscriptions. Regenerate OpenAPI/client types after routes stabilise, run broad backend/frontend checks, and continue later source, analysis, experience and acceptance packets. No live source or model acceptance is implied by these offline tests.

## 2026-09-14: subscription controls, presets and report review

- S03 monthly accounting now retains settled one-off usage after a job is deleted, and holds unknown or unsettled reservations across rollover. The owner and subscription usage endpoints disclose only authorised UTC-month aggregates. Combined backend checks passed 77 tests; the final accounting/API suite passed 16, and affected frontend checks passed 10. Ruff, strict mypy, frontend typecheck, ESLint and Prettier passed. Legacy receipts without a dispatch timestamp may have inexact historical cross-month attribution. Shared-fetch attribution awaits E04.
- S04/R01 schedule integration added exact-revision `POST /api/schedules/from-brief`, local-time recurrence, overlap, catch-up and due-slot identity. The v4 frozen job/request retains all 12 authored requirements and stable provenance. Unsupported lens, exclusions, sections, visuals, monitoring, user limits and required-coverage flags return field-specific 422 responses until production can honour them. The combined schedule/edition/worker suite passed 62 tests, and focused brief conversion/API checks passed 74; Ruff, mypy and the two import-layer contracts passed. Disposable SQLite was used; no live or operator database migration ran.
- R02 shared frontend brief editor and exact saved-revision subscription form passed 25 focused tests plus TypeScript and ESLint. R03 backend now exposes 20 versioned editable presets with source-readiness gaps and required-input disclosure. Its 33 dedicated tests passed, with 97.81% scoped coverage; Ruff, mypy and Bandit passed. The preset picker is in progress.
- E00 is complete as an offline capability contract: 119 catalogue entries are distinguished from 13 typed executable bundles and 16 explicit gaps, with 29 tests and 99.45% scoped coverage. A catalogue entry is not a tested live provider or independent publisher. E01's deterministic allocator is being developed separately; R04's saved-brief preflight is being implemented.
- S05 includes authorised paginated edition history, linked report actions and private monthly usage visibility. S06 now includes an administrator-only queue diagnostic aggregate for due lag, active/blocked editions, uncertain outcomes and saturation. Its focused test passed. Notification delivery, full lifecycle/fairness evidence and operations guide remain open.
- A03 now computes automated release status after frozen citation and evidence assessment, then projects material review reasons and challenge alternatives into the common report document, Markdown, Word and PDF. A document projection version preserves older frozen report rendering. The focused report/subscription suite passed 61 tests, including a legacy projection case. Full A02 adjudication, chart/export visual acceptance and human review remain open.
- A brief with explicit UTC observation dates previously failed strict saved-codec validation for an equivalent `+00:00` offset. A failing API regression reproduced it; the boundary now normalises bounded timezone-aware dates to canonical `Z`. The focused brief API suite passed. No raw credentials or live-provider calls were used in these checks.

Next integration action: finish R03/R04 frontend and preflight, regenerate OpenAPI/types, complete E01 integration, and run broader backend/frontend checks. S01 PostgreSQL migration, notification delivery, source live checks, human-labelled evaluation and browser/export acceptance remain unverified.

## 2026-09-14: source allocation, subscription controls and bounded source bridges

- E01 now uses the reviewed deterministic allocator in collection and replanning rather than only in preview. It filters current provider admission, preserves exact variants and records exclusion receipts. The allocator policy was bumped to v2 because explicit operator terms now contribute to reviewed lexical relevance when the main question is generic; its fixture and regression were updated. The source-allocation, collection and composition suite passed 63 tests. The E01 worker reported 123 production-style tests and 33 final allocation/replan tests passing, with Ruff and mypy passing. The durable source-phase ledger and lease-fenced reserve/settle/abandon checkpoint port passed 28 focused SQLite tests, but seeding and collector enforcement are still being integrated. The pure ledger independently passed 12 tests with 91.67% scoped coverage.
- E02 has a bounded candidate/policy/parser/passage core: 136 offline tests and 99.60% branch-aware scoped coverage, with Ruff and mypy passing. A guarded original transport and selected-passage freeze seam are under development. No original-page collection is yet wired to production reporting.
- A01 has a claim-scoped source assessment and conservative origin-chain domain model. Its 108 focused tests, Ruff and mypy passed after independent review and repair. Production resolution, persistence and historical report projection remain outstanding. A02 currently has only initial weak-confidence-rationale validation; the doctrine/report suite passed 40 tests.
- S05 now has authorised paginated history, run-now with stable retry UUID, edition pause/resume/retry, in-app activity events, and schedule-wide pause/resume including brief-linked subscriptions. The schedule backend worker reported 14 focused tests and import, Ruff and mypy checks passing. The frontend contract/types were regenerated and the focused subscription UI suite passed 19 tests. Typecheck, ESLint and a production build passed. An older form test expected an invalid submission button to be disabled; the existing accessible validation flow intentionally leaves it clickable and shows the missing area-consent issue. The test now asserts that behaviour and that no request is sent.
- S06 fair due selection uses bounded 16-schedule and 16-edition batches per tick with a rotating owner cursor. The worker reported 27 tests, Ruff, mypy and import checks passing. The operations guide was updated. Source-failure category diagnostics and repeated lifecycle/recovery acceptance remain open.
- E03 offline Cloudflare Radar and ONS CPIH bridges are in progress. Their contracts must respect the one-request ResearchProvider port and licensing/approval rules before capability composition. No live provider calls, commercial-use acknowledgement, licence approval or source-secret verification occurred in this milestone.
- A full backend test run and full frontend test run were started but interrupted while agents were actively editing shared imports and multiple suites contended for the same machine. The frontend run exposed one stale area-consent test, which was updated and then passed in the 19-test focused rerun. Those interrupted runs are not acceptance results; repeat broad checks once the integration files settle.

Next integration action: complete E01 frozen ledger enforcement and E02 guarded transport/freeze, resolve E03 one-request adapter contracts, regenerate API types after the accept-baseline route settles, then run sequential backend/frontend checks. PostgreSQL migration, current live providers/models, human-labelled quality, browser flows and export rendering remain unverified.

## 2026-09-14: frozen assessment, reporting regressions and reader controls

- A01 now captures an exact-version, unassessed source-assessment projection in production and validates it on SQL write/read. Legacy versions remain absent; malformed present snapshots fail closed. This does not import trusted source reviews or change report confidence. The A01 worker reported 25 new regression cases across runs, Ruff, mypy, Bandit and import checks passing. Its broader run found three old `test_reports.py` expectations of Ready for synthetic headlines without original source context. Those tests now correctly assert Needs review and the full isolated `test_reports.py` file passed 5 tests.
- E01 fresh-web discovery has a frozen opt-in allocation of one request, at most three tool calls, 90 seconds and 16,000 output tokens, separate from source acquisition. The worker reported 43 focused and 174 broad offline tests passing. One older web HTTP test expected drafting from zero captured evidence; its corrected abstention assertion passed in an isolated rerun. This does not make generated web context citable evidence.
- S05's exact-edition analytical baseline acceptance and S06's allowlisted source-failure diagnostic categories are now in the API contract. A feed-disabled restart test retained the same edition/job IDs without duplicate admission. OpenAPI and frontend types were regenerated. Focused subscription UI checks passed 20 tests across four files; TypeScript and production build passed.
- The subscription list now combines status, topic/question and cadence filters. Its 10-test control-panel suite, ESLint and Prettier passed. The report reader now offers collapsible section navigation on narrow screens; its focused interaction test, ESLint and Prettier passed.
- E03 offline ONS CPIH and Cloudflare Radar adapters are composed into research with recorded-time/units, current key and explicit non-commercial acknowledgement checks. The combined source suite passed 85 tests. No current endpoint/secret or commercial-use acceptance is implied.
- A04's pure six-state change model and X04's exact typed calculator core have focused test evidence, but neither is a complete user-facing production feature. V01 synthetic corpus and E04 bounded selected index are being built separately. The [acceptance record](ACCEPTANCE.md) names outstanding human, live, migration, browser and export gates.

Next integration action: finish the E02 selected-original production path, E04/A04 persistence joins and staged brief UI, then rerun broad checks on a settled code snapshot. A V01 compatibility run found a changed research replay stage count; diagnose rather than editing the expected sequence without understanding the additional request.

## 2026-09-14: replay repair, comparison history and staged research UI

- The replay difference was a genuine duplicate call: an `unavailable` first-pass provider counted as a merge placeholder and was wrongly treated as unattempted in pass two. A distinct unattempted set now prevents an identical retry while leaving the receipt merge semantics intact. The new regression plus replanning/replay suites passed 34 tests; Ruff passed.
- A04 now persists exact-edition comparisons, exposes safe states and reasons from authorised history, and shows a concise comparison in the subscription reader. Root's disposable-SQLite integration suite passed 40 tests; the dedicated frontend history suite passed four. Semantic claim matching, horizon changes and notification policy remain outside this milestone.
- X01's four-step Brief, Scope, Depth and Run editor passed 29 focused tests, with six preset tests rerun after the last edit. TypeScript, ESLint and Prettier passed. The mobile report contents control passed its interaction test. Browser visual/keyboard acceptance remains pending.
- V01's offline corpus now has 60 synthetic cases, 10 each across six domains, and 24 consecutive-edition comparisons. Its saved contract run passed 60/60 cases and 194/194 checks; the author reported 98 focused tests and 98.54% scoped coverage. These are assistant-authored synthetic fixtures, not independent human labels or current-model quality scores.
- E02's original-passage API was registered under the shared router; the targeted original-passage/persistence/production follow-through suite passed 36 tests and router Ruff passed. E02's agent is finishing migration and static checks. No reviewed original-source policy is configured by default, so this code does not silently fetch arbitrary pages.
- A full frontend Vitest run with two workers reported two camera interaction failures and then stopped making progress; it was interrupted and is not a pass. Frontend TypeScript passed. Repeat the suite in bounded groups on a stable snapshot and keep the unrelated camera failures distinct.

Next integration action: wire the E04 selected subscription collector after its owner supplies the tested container signature, regenerate OpenAPI/client types after E02 stabilises, then run broad backend/frontend checks and inspect browser/exports. The full plan remains in progress.

## 2026-09-14: selected history, reviewer lineage and report Q&A

- E02's guarded selected-original route, durable reservation and exact-version passage read were integrated. Root's focused original-passage/production suite passed 36 tests. The deletion gap found in review was fixed for linked and staged passages, with 12 adjacent report/worker regressions passing. No reviewed source policy is configured by default, so arbitrary original fetching remains disabled. Multi-requirement mapping and correction governance remain.
- E04's selected USGS-authored earthquake index now runs in the independent schedule loop with cursor/overlap, bounded acquisition and receipt persistence. The owner reported 19 focused tests and root's container/lifecycle check passed seven. This does not establish broad 400-day or annual coverage; other providers and retention policies remain to be implemented.
- A04's exact-edition comparison is persisted and shown in authorised history. Root's disposable-SQLite integration passed 40 tests and the focused UI passed four. A05's tier-scaled synthesis and structural requirement accounting passed 89 focused tests, without a whole-stage budget or human quality result.
- X05's exact saved-version Ask Eye answer path and report-reader UI now use frozen claims/evidence, recheck access and abstain when nothing matches. The backend API/context suite passed 32 tests. The frontend slice passed 23 focused tests. The later grouped frontend run passed 508 tests across 105 Research, Reports and Ask Eye files. Saved report chats are deliberately disabled because the current saved-chat API supports map scopes only; entity aliases and relevance profiles remain.
- A01's authorised A–F reliability and 1–6 credibility review history, separate issuer-authenticity decision and immutable exact-version snapshot path passed 21 focused tests. A mixed valid/invalid judgement case and cross-team permission guard were included. Production score selection, canonical report reader/export use and reviewer UI remain. Additive migration `0043` follows the subscription archive migration `0042`; `uv run alembic heads` showed one head.
- The archive migration replaced physical subscription deletion with a tombstone that retains edition/index children and authorised history while fencing future work and active leases. Its owner reported 27 adjacent tests, including foreign-key-enabled SQLite. PostgreSQL migration and concurrency acceptance remain unverified.
- E03 added bounded ECB daily GBP/EUR and opt-in IODA outage research bridges alongside Radar/ONS. The combined offline source suite passed 65 tests. Read-only current API shape checks returned HTTP 200 for both; the IODA query was empty and its general reuse terms remain unclear, so operator acknowledgement is off by default. Details are in [E03_BRIDGE_VERIFICATION.md](E03_BRIDGE_VERIFICATION.md).
- Regenerated OpenAPI and TypeScript types after the route additions. The 508-test frontend group, typecheck, ESLint and production build passed. The local browser was reachable but only showed login, with no disposable authorised account available for an authenticated journey. The build still reports large map/library chunks.
- `pnpm audit` initially found a high-severity transitive `js-yaml` 4.3.1 advisory. A pnpm-workspace override and refreshed lockfile now select 4.3.2; the rerun reported no known vulnerabilities. `uv run pip-audit` reported none in published backend dependencies, while skipping the local unpublished `ase` package. No runtime credential was printed or changed.

Next integration action: finish the lease-fenced E05 challenge-settlement regression, then run broad backend static/test/security checks and update the acceptance record with exact failures or passes. Current model, human-labelled quality, PostgreSQL, authenticated browser and rendered export gates remain unverified.

## 2026-09-14: final doctrine, ledgers and authenticated browser smoke

- E05 selected challenge-source results now settle atomically with lease-fenced usage so a restart reuses known evidence instead of redispatching it. Eight focused tests and a 133-test adjacent Research/Subscriptions backend integration run passed on disposable SQLite. The complete 676-file backend suite was attempted but stopped after slow progress; it is not a passing release gate.
- A02 revalidates the final body after challenge and advocacy. New doctrine errors force Needs review, including numeric or forbidden likelihood prose outside judgement cards. The 29-test focused suite passed. Production still lacks typed forecast horizon/resolution/review fields and exact frozen original-passage links in its report packet, so those validators cannot yet be invoked honestly.
- A04 treats swapped or reidentified claim mappings as unresolved rather than reporting a definitive assessment change. The 46-test focused suite passed. Text fingerprints do not prove semantic equivalence.
- X03 added migration `0044`, exact reviewed-claim forecast/indicator persistence and an authorised API. The latest focused domain, API and migration set passed 30 tests; a wider local combination passed 82. The API intentionally accepts only unresolved forecast reviews and explicit missing readings until independent post-issue evidence can support true/false resolution and numeric observations. No operator database migration ran.
- A separate QA backend/frontend pair used only a disposable SQLite user and ports 8015/5175. Login, Research and Subscriptions loaded in the browser; the initial 422 was traced to a reserved `.invalid` test email domain and resolved by using a normal test address. The QA servers and test database were removed. No paid model call or real subscription was created. Desktop screenshots were inspected; narrow, keyboard, reduced-motion and report-export journeys remain.
- Subscription controls now say Archive, matching backend tombstone/history behaviour, and the page describes daily through annual cadence accurately. Seven focused UI tests, frontend typecheck and lint passed. Other frontend work had previously passed 508 focused tests, typecheck, lint and production build.
- Frontend and backend dependency audits pass after updating the transitive `js-yaml` lock. Bandit passes after narrow, documented annotations for defused XML parsing and isolated PDF worker paths. The security note records scope and outstanding gates. A report reviewed-source projection is still being integrated; regenerate OpenAPI/types and rerun combined checks after it settles.

Next integration action: finish the exact-version reviewed-source reader/export projection, regenerate OpenAPI and TypeScript, run settled report/ledger/static checks, then continue the remaining production scoring, subscriptions and live acceptance paths. The plan remains in progress.

## 2026-09-14: selected-source reader and ledger security integration

- A01 now supports an explicitly selected, authorised `source_snapshot_id` with an exact report version in the canonical reader and Markdown, Word and PDF exports. It adds a separate reviewed-source section without mutating frozen judgements or the original source grades. The worker's nearby regression group passed 35 tests.
- Independent read-only review found two X03 ledger defects. Expiry during the final commit could release private data, and a head-only database change could misattribute a hashed forecast to a different claim revision. Both new regressions failed before repair. Final response expiry checks and decoded-payload/head bindings now pass the 84-test ledger domain, API and migration run. Indicator citation is frozen in the first persisted version and compared with its head. Full malicious database-writer resistance is outside the hash model.
- The nine-file report, source-review, document and ledger integration suite passed 58 tests on disposable SQLite. `uv run alembic heads` showed a single `0044` head. Backend Ruff, format, strict mypy (1,140 source files), import contracts, Bandit and `git diff --check` passed after integration. OpenAPI and TypeScript API types were regenerated; frontend typecheck and 12 affected tests passed. Production build passed after the subscription UI adjustment, before regeneration of type-only API contracts.
- The full backend test suite, current paid-model runs, human-reviewed analytical labels, PostgreSQL upgrade/races, complete browser journey and rendered multi-format exports remain release gaps. Research/Subscriptions changes are uncommitted on `main`; no operator database was migrated or production environment changed.

Next integration action: wire validated reviewer grades into production scoring only with an explicit frozen-selection policy, add trusted post-issue observations before resolving forecasts, and finish subscription delivery/notification controls. Run the remaining operational quality gates before a release claim.

- S05 added a Duplicate control for ordinary subscriptions. It pre-fills the existing form, changes the name, saves through the create route rather than the update route, and starts the copy paused to avoid an unrequested run. The first two-file focused UI run passed eight tests, including preservation of the original subscription.
- Linked Research Brief subscriptions now load their exact saved revision, pre-fill a recurrence-only copy form and create the new schedule paused through `/api/schedules/from-brief`. The copy verifies the returned revision and paused state, while the original remains untouched. The focused copy and brief-workspace files passed ten tests and TypeScript passed. Notification policy, delivery and full browser acceptance remain.
- The subscription list's filtering and headline calculations moved to a 55-line helper so the page component stays below the 350-line target. The three-file copy, refresh and brief-workspace group passed 15 tests after replacing one immediate post-navigation assertion with an awaited editor query. That grouped run spent nearly ten minutes in Vitest setup/transform, so it is not a performance acceptance result. Final TypeScript, scoped ESLint and `git diff --check` passed.

## 2026-09-14: report-scoped private Ask Eye conversations

- X05 now persists the exact report id and edition number beside every saved report-scoped Ask Eye turn. The API rejects missing, mixed or tampered report metadata, rechecks report visibility when saving and reopening a chat, strips transient continuation/model fields as it already does for map chats, and preserves the frozen answer context on resume.
- The Eye assistant can save, resume and continue a report conversation without re-opening a newer edition or sending map filters by mistake. The focused frontend slice passed 17 tests, including API boundary validation, the saved-chat journey and mismatched-edition guard. The saved-chat API file passed six tests against disposable SQLite with `--no-cov`; the normal repository coverage gate is intentionally not a release result for a narrow file run.
- OpenAPI and generated TypeScript contracts include the `report` saved-turn scope and explicit report reference. Entity aliases, relevance profiles, live provider/model acceptance, full browser journeys, PostgreSQL and rendered export gates remain outstanding.

## 2026-09-15: integration review, full-suite repairs and merge to main

Another session reviewed the uncommitted Research and Subscriptions work before merging it to
`main`, ran the complete backend suite (all 684 files, eight parallel runs) and the complete
frontend suite with coverage, and classified every failure against a clean worktree of the
previous commit.

- Ten backend failures already existed on `main` before this work and are unchanged (planning
  eligibility, direction advocacy, input declaration, photo geolocation schema, one fixed
  interval case, research input capacity, SEC filing capacity, unknown publication, embeddings
  identity encoding, and a WSDOT registry count that was corrected here).
- Production defects repaired: historical export and comparison digests had changed because new
  ReportVersion fields (brief, requirements, document schema, source assessment) and an empty
  research `original_followup` list entered canonical bytes; they are now omitted at their
  defaults. Collection checkpoints had silently accepted schema versions other than 1 (including
  `True`) after the shared boundary wrapper change; the decoder pins version 1 again. Area
  research excluded its own area-bound sources: questions such as "What is happening here?"
  matched no reviewed keywords, and Copernicus acquisition footprints declared only recorded
  dates while area research defaults to acquisition-or-publication time. Area-bound sources and
  explicitly selected sources now count as relevant, and Copernicus declares research and
  recorded intervals.
- `report_jobs/service.py` exceeded the 400-line gate; discard rules moved to `controls.py` and
  initial payload construction to `admission_payload.py`, without behaviour change.
- Stale tests updated with reasons recorded beside each change: the citation review gate
  (needs review for thin synthetic evidence), the checkpointed collection path, the accepted
  baseline field, the Archive wording, the 121-provider registry, settings-derived AidData
  requirements, per-version source-assessment receipts, subscription edition intervals without
  research mode, the ONS subject, and merged replan receipts. Several tests had silently stopped
  exercising their guard because the pipeline no longer calls the model without evidence; they
  now seed evidence so revoked membership, deep model JSON, provider-error redaction and client
  disconnect cancellation are exercised again, and each still passes.
- Frontend: tests added for the brief library, Ask Eye answer copying and map actions, edition
  history states and the map context hook, lifting branch coverage from 89.73% to 90.06%; one
  brief editor race was removed.
- The operator's development SQLite database was backed up and verified
  (`backend/data/backups/sqlite-pre-0044-20260915-023726`), the upgrade rehearsed on a copy with
  identical row counts, then migrated from `0034` to `0044` with integrity and foreign-key checks
  passing. PostgreSQL upgrade, current-model quality, human labels, complete browser journeys and
  rendered exports remain the release gaps named above.
